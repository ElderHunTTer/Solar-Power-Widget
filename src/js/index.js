import '../css/widget.css';
import SolarQuoteWidget from './widget';

// Export for both module and script tag usage
export default SolarQuoteWidget;

// Make available globally when included via script tag
window.SolarQuoteWidget = SolarQuoteWidget;